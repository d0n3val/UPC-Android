<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andriod.upc.exercici_registre" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".NouRegistre"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MostrarResultat"
            android:label="@string/title_activity_mostrar_resultat"
            android:parentActivityName=".NouRegistre" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.andriod.upc.exercici_registre.NouRegistre" />
        </activity>
    </application>

</manifest>
